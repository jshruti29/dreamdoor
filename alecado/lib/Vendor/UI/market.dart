import 'package:flutter/material.dart';

class market extends StatelessWidget {
  const market({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("This Market Page"),
      ),
    );
  }
}
